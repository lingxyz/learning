# Number 数据类型比较的注意点：
```js
// NaN与其他任何值都不相等，包括它自己
NaN === NaN    // false

// 浮点数在运算过程中会产生误差，因为计算机无法精确表示无限循环小数
1 / 3 === (1 - 2 / 3);    // false

// 要比较两个浮点数是否相等，只能计算它们之差的绝对值，看是否小于某个阈值（校正的最小输入值）：
Math.abs(1 / 3 - (1 - 2 / 3)) < 0.0000001;    // true
```
